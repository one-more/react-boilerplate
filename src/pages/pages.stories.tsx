import { LoginPage as LoginPageComponent } from '~/pages/login';

// eslint-disable-next-line import/no-default-export
export default {
    title: 'Pages',
    parameters: {},
};

export const LoginPage = LoginPageComponent;
